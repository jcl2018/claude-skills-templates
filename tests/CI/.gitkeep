# tests/CI/ — the `CI` category dir (F000074 category-based test contract).
#
# V1 FOUNDATION: this dir establishes the on-disk category layout the contract
# declares (spec/test-spec-custom.md categories:), so /CJ_test_audit's
# --check-structure (b) check passes. The PHYSICAL move of the CI-category test
# scripts (the *.test.sh suites + validate / test-deploy / windows-smoke) INTO
# this dir is a DEFERRED follow-up — this PR only lands the additive category
# axis. Test-script discovery in scripts/test.sh + the anchor paths still point at
# the current flat locations; do NOT move scripts here until the deferred migration.
