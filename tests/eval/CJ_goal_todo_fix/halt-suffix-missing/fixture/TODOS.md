# TODOS

## Active work

### Replace tabs with spaces in some files

This TODO heading is missing the `(P[1-4], [SMLX]+)` priority/size suffix.
The /CJ_goal_todo_fix pre-flight gate requires explicit annotation — domain-grouped or
flat-active mode both refuse missing suffixes per the source design. Body is
long enough to pass the 50-char gate and contains no sensitive surface or
design-needed keywords; the only gate this should hit is the suffix-missing
halt.
