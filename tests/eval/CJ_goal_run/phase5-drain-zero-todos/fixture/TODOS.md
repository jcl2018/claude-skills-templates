# TODOS

## Active work

### Pre-existing TODO from a prior run (P2, M)

This row was on `main` before the simulated feature PR landed. Phase 5 only
counts headings ADDED by the merged PR; this row is unchanged so it does NOT
count toward `new_todos_count`.

## Deferred work

(no entries)
