# Test Plan: Collection Versioning

Generated by /autoplan eng review on 2026-04-10
Branch: main

## New Tests Required (extend scripts/test.sh)

### collection-version.sh smoke tests
1. `get` returns valid semver from VERSION file
2. `bump patch` increments patch (0.1.0 -> 0.1.1)
3. `bump minor` increments minor (0.1.0 -> 0.2.0)
4. `bump major` increments major (0.1.0 -> 1.0.0)
5. `manifest` produces valid JSON with required fields (collection_version, commit, skills, templates, skill_dependencies)
6. `manifest` templates array has sha256 checksums for every file in templates/

### validate.sh VERSION checks (negative tests)
7. Missing VERSION file -> FAIL
8. VERSION contains non-semver string -> FAIL
9. VERSION < latest v-tag -> FAIL (regression detection)

### validate.sh VERSION checks (positive tests)
10. VERSION >= latest v-tag -> PASS
11. No v-tags exist -> PASS (conditional check)

### skill-ship.sh integration (extend existing ship test)
12. Collection v-tag created alongside skill tag
13. VERSION file incremented after ship
14. CHANGELOG.md updated after ship
15. test cleanup handles both commits + both tags

### skills-deploy integration
16. install records collection_version in manifest
17. doctor reports version status
