# Test Plan
Generated by /plan-eng-review on 2026-04-11
Branch: main
Repo: jcl2018/claude-skills-templates

## Verification Steps

1. Run `./scripts/validate.sh` — must pass with no workflow/contracts references
2. Run `./scripts/test.sh` — must pass, including workflow_type absence check
3. Run `./scripts/test-deploy.sh` — must pass with docs as fixture (rewritten tests)

## Manual Dogfood Tests

After CLAUDE.md rules are in place, verify each type:
- Create a feature work item on a feat/* branch — should scaffold TRACKER + PRD + ARCH + TEST-SPEC + milestones
- Create a defect work item on a fix/* branch — should scaffold TRACKER + RCA + test-plan
- Verify template fallback chain works (templates/ at repo root)

## Edge Cases

- Verify `tracker-user-story.md` frontmatter says `type: user-story` (not `userstory`)
- Verify `skills-deploy install docs` deploys all 13 reassigned templates
- Verify `skills-deploy doctor` reports no orphaned templates

## Critical Paths

- Deletion + validate.sh passing = catalog/filesystem consistency
- test-deploy.sh passing = deploy pipeline still works with docs as fixture
- Manual dogfood = CLAUDE.md rules produce correct work item structure
