LAND & DEPLOY REPORT
═════════════════════
PR:           #61 — v1.5.2 fix: skills-deploy install — make --overwrite the default (D000015)
Branch:       fix/skills-deploy-overwrite-default → main
Merged:       2026-05-08T05:00:02Z (squash merge per CLAUDE.md convention)
Merge SHA:    2717b2e
Merge path:   auto (gh pr merge --auto --squash --delete-branch)
First run:    no (previously confirmed)

Timing
  Dry-run:    skipped (confirmed)
  CI wait:    none (PR checks were green at invocation)
  Queue:      none (auto-merge w/ no merge queue on this repo)
  Deploy:     n/a (no cloud deploy — template workbench)
  Staging:    n/a (no staging environment)
  Canary:     local sanity (grep-verified key strings on main)

Reviews
  Eng review: CURRENT (just-ran in /ship — checklist + adversarial subagent + 6-pass coverage audit)
  Inline fix: yes (1 fix — test-deploy.sh T6 split into 3 sub-cases)

Pre-merge readiness
  CI:                 PASSED (validate + GitGuardian Security Checks)
  scripts/test.sh:    PASS (0 failures — re-run at readiness gate)
  scripts/validate.sh: PASS (0 errors, 0 warnings)
  VERSION drift:      none (main=1.5.1, branch=1.5.2, no other PRs in queue)
  CHANGELOG/VERSION:  updated (v1.5.2 entry, bumped 1.5.1 → 1.5.2)
  CLAUDE.md:          updated (Template deployment bullet)
  PR body:            current (authored by /ship from latest commit)
  Mergeable:          CLEAN

Deploy
  Strategy:           none (template/library repo — no Fly/Render/Vercel/Netlify/Heroku)
  Push CI on main:    none configured (workflows are pull_request-scoped)
  D000013 post-merge hook: not triggered for this PR (diff did not touch
                          templates/, skills/, skills-catalog.json, or rules/ —
                          hook gates on those paths). No auto-redeploy needed.
  Local main canary:  PASS
                        - `local skills=() overwrite=true` present (default flipped)
                        - `--no-overwrite) overwrite=false ;;` handler present
                        - `UPDATE: $tpl (checksum differs)` at scripts/skills-deploy:382
                        - `PRESERVE: $tpl ... (--no-overwrite ...)` at scripts/skills-deploy:387 (template)
                        - `PRESERVE: $rule_name ... (--no-overwrite ...)` at scripts/skills-deploy:422 (rules)
  Remote branch:      deleted (--delete-branch succeeded)
  Local branch:       deleted (gh checked out main and removed feature branch)

VERDICT: DEPLOYED AND VERIFIED (template repo — merge IS the deploy)

Follow-ups
  - Pre-existing scripts/test-deploy.sh failures on main (Tests 1, 4, 13, 14, 15
    re: deprecated company-workflow handling) are unrelated to D000015 and worth
    a separate defect later.
  - Adversarial subagent flagged silent-overwrite UX (no .bak, no preview) and
    a pre-existing manifest checksum quirk under --no-overwrite. Both deferred
    to D000015's "Out of scope" section in the tracker; revisit if user reports
    accidental data loss.
  - On user machines that pull main, D000013's post-merge hook will keep firing
    when templates/skills/catalog/rules change. The hook still passes --overwrite
    (now a no-op) so it works unchanged.
