# Test Plan
Generated by /plan-eng-review on 2026-04-17
Branch: feat/scaffolding-hierarchy-rules
Repo: jcl2018/claude-skills-templates

This is a CLI tool change, not a web app change. /qa cannot exercise it via browser. Test execution happens in `scripts/test.sh`.

## Affected Pages/Routes
N/A — CLI tool. No URLs.

## Key Interactions to Verify (test against scaffolder CLI)
- `scripts/scaffold-work-item --skill personal --type defect --name foo` — succeeds, atomic dir creation, no tempdir leftover
- `scripts/scaffold-work-item --skill company --type feature --name foo` — fails with clear error (≥1 user-story required)
- `scripts/scaffold-work-item --skill company --plan plan.yaml` — succeeds for fully-nested tree
- `scripts/scaffold-work-item migrate --skill personal --dry-run` — prints planned moves
- `scripts/scaffold-work-item migrate --skill personal --apply` — executes renames, idempotent on second run
- `./scripts/test.sh` — full suite passes including new "Regression test (FXXXXXX)" block

## Edge Cases
- Concurrent scaffolder invocations (flock prevents ID collision)
- Template substitution with literal `{custom}` placeholder not in allow-list (must pass through untouched)
- Migration on bare-slug dir without TRACKER.md (skip + log + non-zero exit)
- Migration on bare-slug dirs with same TRACKER `id` field (data corruption — fail loudly, exit 2)
- Inferred ID collides with existing properly-named dir (skip + log)
- Process killed mid-scaffold (tempdir leftover — document cleanup script needed)

## Critical Paths
- AI scaffolds → atomic tree appears → validator passes → no orphans possible
- Developer migrates legacy repo → dry-run → review → apply → validator passes after
- Both skills' validators reject the same invalid fixtures with the same verdicts (consistency)

## Out of Scope for This Plan
- Browser/UI testing (no UI exists)
- Performance/load testing (CLI tool, runs once per scaffold)
- Cross-OS testing beyond Mac/Linux (Windows tested via existing `skills-deploy` patterns; bash assumed)
