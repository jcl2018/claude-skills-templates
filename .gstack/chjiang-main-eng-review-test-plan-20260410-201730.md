# Test Plan
Generated by /plan-eng-review (via /autoplan) on 2026-04-10
Branch: main
Repo: jcl2018/claude-skills-templates

## Affected Files
- `skills/system-health/SKILL.md` — full rewrite, 5-step graph-first skill
- `skills/system-health/DESIGN.md` — update from stub
- `skills/system-health/CHANGELOG.md` — v0.2.0 entry
- `skills-catalog.json` — waza dependency declaration
- `docs/system-health/PRD.md` — rewrite
- `docs/system-health/ARCHITECTURE.md` — rewrite
- `docs/system-health/TEST-SPEC.md` — rewrite

## Smoke Tests (automated, scripts/test.sh)
1. SKILL.md exists with valid frontmatter (name, description, version, allowed-tools)
2. skills-catalog.json has waza in system-health's depends.skills
3. Doc triplet exists: docs/system-health/PRD.md, ARCHITECTURE.md, TEST-SPEC.md
4. DESIGN.md is not a stub (has content beyond placeholder)
5. CHANGELOG.md has v0.2.0 entry

## Key Interactions to Verify (manual E2E)
1. Run `/system-health` in a Claude Code session — all 5 steps produce output
2. Verify scan command correctly identifies all installed skills in ~/.claude/skills/
3. Verify graph analysis detects known broken symlink (cross-retro)
4. Verify waza integration produces config health output
5. Verify scoring produces 0-10 composite score
6. Run twice — second run shows trend delta

## Edge Cases
1. Waza not installed — graceful skip message, no error
2. Empty ~/.claude/skills/ — handles gracefully (no crash on empty glob)
3. Settings.json missing — skill continues without settings data
4. Corrupted health-history.jsonl — falls back to "no prior data"
5. Large skill dir (gstack, 80+ files) — scan completes in <5s

## Critical Paths
1. Scan → Graph Analysis → Score: the full pipeline must produce consistent output
2. Waza degradation: must not crash when waza is absent
3. History persistence: must append valid JSONL line
