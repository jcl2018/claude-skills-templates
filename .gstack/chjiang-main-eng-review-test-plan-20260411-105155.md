# Test Plan
Generated by /plan-eng-review on 2026-04-11
Branch: main
Repo: jcl2018/claude-skills-templates

## Verification Steps (post-implementation)

### Automated (test.sh)
- `./scripts/validate.sh` — catalog entries match files on disk
- `./scripts/test.sh` — full smoke + integration suite
- New: template content smoke tests (S000002 TEST-SPEC S1-S4, S6)

### Manual
- `/contracts check` on F000001 doc triplet at `.claude/worktrees/old-skills/work-items/F000001_workflow_alpha/`
- Phase detection: on a feat/* branch with an active work item, `/workflow` should still show correct phase

## Key Interactions to Verify
- `./scripts/validate.sh` passes after simultaneous file deletion + catalog update
- `./scripts/test.sh` passes including new template smoke tests
- Template placeholders ({FEATURE_NAME}, {YYYY-MM-DD}, etc.) still present in updated templates

## Edge Cases
- Stale templates at `~/.claude/templates/` — need `skills-deploy install --overwrite` after merge
- Worktree copies (.claude/worktrees/) have their own skills-catalog.json — not updated by this change (they get it on rebase)

## Critical Paths
- validate.sh: catalog references deleted files -> FAIL if not updated in same commit
- test.sh: new smoke tests verify no enterprise gates survive in templates
