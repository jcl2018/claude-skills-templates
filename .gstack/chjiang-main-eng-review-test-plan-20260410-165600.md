# Test Plan
Generated by /plan-eng-review on 2026-04-10
Branch: main
Repo: jcl2018/claude-skills-templates

## Test Runner
`./scripts/test.sh` — the test suite itself is a deliverable. No external framework.

## Critical Paths
- `create-skill.sh` happy path: scaffold produces valid SKILL.md + doc triplet + catalog entry
- `create-skill.sh` guard: duplicate name rejected with exit 1
- `create-skill.sh` guard: invalid name (special chars) rejected with exit 1
- `validate.sh` happy path: clean repo exits 0
- `validate.sh` negative: orphan directory detected, exits 1
- `test.sh` integration: scaffold → validate → cleanup (temp copy restore)

## Smoke Tests (advisory scripts)
- `doctor.sh` runs without crash (exit 0)
- `lint-skill.sh` runs without crash (exit 0)
- `deps.sh` runs without crash, output contains known skill names
- `generate-readme.sh` produces output containing skills table header
- `generate-readme.sh` idempotency: run twice, diff outputs

## Edge Cases
- `create-skill.sh` with name containing `../` or spaces → rejected
- `validate.sh` on empty catalog (`[]`) with existing skill dirs → orphan detected
- `deps.sh` on skills with no dependencies → standalone classification
- `sync-upstream.sh` without gstack installed → warns and exits 0
