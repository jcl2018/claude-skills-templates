# Test Plan
Generated by /plan-eng-review on 2026-04-11
Branch: claude/nostalgic-volhard
Repo: claude-skills-templates

## Affected Pages/Routes
- `templates/company-workflow/` — verify all 13 templates byte-identical to spec source
- `skills/company-workflow/` — verify SKILL.md, contract.json, reference/, philosophy/, fixtures/ present
- `template-registry.json` — verify JSON validity, both sets declared, paths point to real dirs
- `skills-catalog.json` — verify company-workflow entry added, validate.sh passes

## Key Interactions to Verify
- `company-workflow validate <path>` on a valid company work item (expect exit 0)
- `company-workflow validate <path>` on invalid fixture (expect exit 1 with violation list)
- `./scripts/validate.sh` after all changes (expect exit 0)
- `./scripts/test.sh` after all changes (expect exit 0)

## Edge Cases
- Registry paths point to nonexistent directory (should be caught by smoke test)
- SKILL.md validate subcommand run on a non-company work item (different template set)
- `userstory` vs `user-story` type spelling in company templates (must be `userstory`, not `user-story`)

## Critical Paths
- Copy 27 source files → create registry → create SKILL.md → add catalog entry → validate.sh passes → test.sh passes
- SKILL.md fallback chain resolves contract.json from both workbench and foreign-repo paths
