# Test catalog — `test` family

<!-- GENERATED FILE — do not edit by hand.
     Rendered from the merged test-spec registry (spec/test-spec.md +
     spec/test-spec-custom.md) by: scripts/test-spec.sh --render-docs
     Re-run that command to regenerate; validate.sh Check 26 enforces freshness. -->

Verification units in the `test` family, rendered from the test-spec
registry. Each row shows only registry-rendered fields; the `anchor` is a
source reference, never a claim.

| Label | Layer | Disposition | Trigger | Source · anchor | Purpose |
|-------|-------|-------------|---------|-----------------|---------|
| audit-skills suite — seed delivery + audit engines | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-audit-skills.test.sh` | Bare-repo seed delivery for both audit skills, second-run idempotence, engine-flagged seeded-violation findings (stage1/ prefixes), the per-stage report contract on both SKILL.mds plus qa.md's block template, the planted-drift stage3 cross-walk drill, and the clean workbench baseline. |
| cj-document-release suite — doc-release skill structure | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-document-release.test.sh` | Doc-release skill structure, frontmatter, halt markers and config-helper assertions. |
| doc-release config suite — doc registry + helper + seed | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-document-release-config.test.sh` | Doc registry table shape, every doc-spec helper subcommand, strict no-config gates (malformed table row, no-table registry), and the byte-identical embedded seed. |
| goal-common portability suite — pre-ship portability gate | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-goal-common-portability.test.sh` | A clean catalog passes, dry-run runs nothing, a dishonest declaration yields findings, and an absent engine skips fail-soft. |
| goal-common recap suite — land/PR 3-part recap formatter | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-goal-common-recap.test.sh` | The --phase recap pure formatter renders all three labelled sections, switches the header on --when before&#124;after, is fail-soft on a missing field, and prints --field content verbatim (no eval). |
| goal-common sync suite — pre-build skills-sync phase | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-goal-common-sync.test.sh` | Dry-run, opt-out, guard-refusal and real-run paths of the pre-build sync phase all emit the four-key schema, fail-soft and hermetic. |
| goal doc-sync wiring suite — symmetric step wiring | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-goal-doc-sync-wiring.test.sh` | The doc-sync step and halt-taxonomy rows are present and correctly ordered in the goal orchestrators. |
| feature-path smoke suite — worktree entry + common phases | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-goal-feature-smoke.test.sh` | Feature-caller worktree entry, the shared helper's worktree/ship/telemetry phases, and leaf dispatch targets present on disk. |
| goal PR-body splice guard suite — no multi-line awk -v payload idiom | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-goal-pr-body-splice-guard.test.sh` | No executable line in any of the four cj_goal pipeline.md passes a multi-line shell payload through awk -v; only the safe --body-file filename idiom and the warning comments remain, and each file keeps its gh pr edit --body-file splice. |
| cj-id-claim suite — atomic work-item ID claim | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-id-claim.test.sh` | Concurrent-race uniqueness, both reap modes, prefix isolation, same-branch reuse and worktree-shared claim-root resolution. |
| cj-task-scaffold suite — task complexity gate + scaffold | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-task-scaffold.test.sh` | Complexity-gate refusals, dry-run preview, live scaffold and idempotency of the task scaffolder. |
| cj-worktree-cleanup suite — post-run worktree janitor | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-worktree-cleanup.test.sh` | PR-state-gated sweep, orphan-dir removal, guard refusals and pipeline seams of the worktree janitor. |
| cj-worktree-init suite — worktree creation helper | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/cj-worktree-init.test.sh` | Caller prefixes, dirty-checkout guard and base-freshness fork behavior of the worktree-init helper. |
| doc-spec overlay suite — two-tier merge semantics | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/doc-spec-overlay.test.sh` | Overlay merge semantics, the duplicate-path guard, merged list subcommands, seed-equals-general-file byte identity, and the --check-on-disk Stage-1 battery (clean fixture: five checks PASS / CHECKS_RUN=5; seeded violations each isolated to its own stage1/<id> finding, including the docs/workflows/ recursed orphan and the registry-gated workflows-subfolder mandate; registry-absent REGISTRY=absent skip; invalid-registry halt). |
| doc-spec reconcile suite — classify + legacy->canonical migration | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/doc-spec-reconcile.test.sh` | doc-spec.sh --classify labeling the four generations (absent/canonical/legacy/duplicate, plus malformed-not-legacy), --reconcile migrating a 40+-row legacy YAML fixture to the canonical Markdown table preserving every row (atomic + .bak + idempotent), the audit_class asymmetry guard (RECONCILE-WARN), the malformed-file no-clobber halt, and the live-workbench canonical-no-reconcile-noise baseline. |
| drain-one-todo suite — unreachable-helper fail-loud | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/drain-one-todo-helper-unavailable.test.sh` | The drain halts loudly when the worktree helper is unreachable instead of scaffolding in place. |
| drain-one-todo suite — deployed-path resolution | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/drain-one-todo-worktree-resolve.test.sh` | A deployed drain helper resolves the worktree-init helper via the manifest source path. |
| post-land-sync suite — post-merge local sync helper | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/post-land-sync.test.sh` | Sync-helper guards refuse a bad source checkout; dry-run previews without mutating the live home. |
| setup-hooks suite — git hook installer | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/setup-hooks.test.sh` | The installed post-merge hook re-deploys skills without mutating trackers; hook install is clobber-safe. |
| test-spec suite — two-tier registry parser + coverage drills | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/test-spec.test.sh` | Merged-registry parser round-trip, the absent-vs-invalid split, malformed fixtures, the units-gated floor note, seed emission, and the temp-dir coverage drift drills. |
| test-spec reconcile suite — symmetric classify + dedup/no-op | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/test-spec-reconcile.test.sh` | test-spec.sh --classify labeling absent/canonical/duplicate/malformed (never legacy — the fenced-yaml format never diverged), --reconcile as a dedup/no-op (canonical clean no-op, duplicate reports the redundant copy with no auto-delete, malformed halts), and the live-workbench canonical-no-reconcile-noise baseline. |
| test-spec render suite — generated catalog renderer + freshness primitive | ci | hard-fail | pr-ci | `scripts/test.sh` · `tests/test-spec-render.test.sh` | The --render-docs renderer emits a deterministic (render-twice byte-identical), work-item-ID-free generated test catalog from the merged registry, and --render-docs --check exits zero on a fresh render and non-zero after a hand-edit — the freshness primitive behind validate.sh Check 26. |
| Inline — advisory-script crash + generator idempotency | ci | hard-fail | pr-ci | `scripts/test.sh` · `Smoke-testing advisory scripts...` | Doctor, lint and deps scripts run without crashing; the README generator is idempotent (temp-only). |
| Inline — catalog + frontmatter + doc-triplet smoke | ci | hard-fail | pr-ci | `scripts/test.sh` · `Checking for duplicate skill names...` | No duplicate skill names; SKILL.md frontmatter parses; doc triplets carry their required sections. |
| Inline — Copilot bundle coverage + round-trip | ci | hard-fail | pr-ci | `scripts/test.sh` · `Checking [id] bundle-artifact-completeness coverage...` | Bundle completeness coverage, the instructions size budget and the deploy round-trip. |
| Inline — goal-common phase integration | ci | hard-fail | pr-ci | `scripts/test.sh` · `Integration test ([id] / [id]): --phase sync end-to-end` | Sync, portability-audit and task-worktree phases of the shared goal helper, end-to-end and hermetic. |
| Inline — handoff-gate deterministic suite | ci | hard-fail | pr-ci | `scripts/test.sh` · `=== [id]: scripts/cj-handoff-gate.sh deterministic tests ===` | Denylist hits, size caps, rename/symlink/test-weakening detection and the QA predicate of the deterministic handoff gate. |
| Inline — harness-principle regression guards | ci | hard-fail | pr-ci | `scripts/test.sh` · `# === [id]/[id]: trajectory-QA regression guards ===` | Static guards that the trajectory-QA, permission-policy and within-phase-receipt fixes stay in place. |
| Inline — install equals clone integration battery | ci | hard-fail | pr-ci | `scripts/test.sh` · `Integration test ([id] / [id]): shared-scripts self-containment...` | Shared-script self-containment, bundle install, develop-in-place and the in-place install-equals-clone contract. |
| Inline — portability-engine hermetic fixture | ci | hard-fail | pr-ci | `scripts/test.sh` · `Integration test ([id] / [id]): cj-portability-audit.sh engine fixture...` | The portability-audit engine's verdicts against a controlled fixture catalog. |
| Inline — defect and story regression battery | ci | hard-fail | pr-ci | `scripts/test.sh` · `Regression test ([id]): Windows jq CRLF wrapper present...` | Shipped defect and story fixes stay fixed: CRLF wrappers, the merge-convention guard, template sync, copy-mode fallback and more. |
| Inline — manual skill-creation integration cycle | ci | hard-fail | pr-ci | `scripts/test.sh` · `Integration test: manual skill creation cycle...` | A scaffolded temp skill keeps the validator green; plant-and-restore negatives prove the doc checks actually fire. |
| Inline — static wiring checks | ci | hard-fail | pr-ci | `scripts/test.sh` · `Checking [id] portable POSIX runtime` | Portable POSIX runtime idioms, registered-doc audit wiring, defect tracker promotion and the workflow-doc Touches blocks. |
| Inline — template content + validator portability + orphan negatives | ci | hard-fail | pr-ci | `scripts/test.sh` · `Checking tracker template content...` | Tracker templates carry required sections; the workflow validator stands alone; orphan-directory detection fires. |
| Inline — test-spec registry + coverage guards | ci | hard-fail | pr-ci | `scripts/test.sh` · `# === [id]: test-spec registry + coverage guards ===` | The test-spec parser validates the merged registry, the coverage cross-check passes on the live tree, and an absent registry classifies as inactive rather than a finding. |
| Inline — backlog append POSIX-clean guard | ci | hard-fail | pr-ci | `scripts/test.sh` · `Checking CJ_improve-queue append path keeps TODOS.md POSIX-clean...` | The improve-queue append path keeps the backlog file POSIX-clean. |
| Inline — full validator re-run | ci | hard-fail | pr-ci | `scripts/test.sh` · `=== Running validate.sh ===` | Runs the whole validator inside the test suite so every check gates the test run too. |
| Inline — version-queue preflight smoke | ci | hard-fail | pr-ci | `scripts/test.sh` · `Smoke-testing scripts/check-version-queue.sh...` | The version-queue preflight runs read-only and degrades cleanly when offline. |
