---
type: spec
parent: S000009
feature: F000004
title: "Always-On Copilot Instructions — Specification"
version: 1
status: Draft
date: 2026-04-22
author: chjiang
reviewers: []
---

<!-- Migrated from S000009_PRD.md + S000009_ARCHITECTURE.md during F000008
     v1.5.0 sweep. Section content preserved from the two source files
     for historical fidelity. The new doc-SPEC.md template suggests
     Problem Statement / Mental Model / ## Requirements (with ### P0/P1/P2
     sub-sections) / Acceptance Criteria / Architecture / Tradeoffs /
     Open Questions; refine over time as needed. -->

<!-- ===== From S000009_PRD.md ===== -->

## Problem Statement

Even with the `/validate` prompt installed (S000007), Copilot has no
context about _how_ the engineer works on work items: the 3-phase
lifecycle, the ID scheme, the hierarchy rules, where to scaffold new
items. Without an always-on context file, the engineer has to re-explain
conventions every session, and Copilot invents directory layouts or
template structures that don't match the spec. A `copilot-instructions.md`
makes the conventions ambient: Copilot knows them by default, and the
engineer can focus on the work itself.

## Mental Model

Three surfaces, different concerns:

```
always-on                         on-demand                     reference (read by prompt)
---------                         ---------                     -------------------------
copilot-instructions.md           validate.prompt.md            copilot-artifact-manifests.json
(S000009 — this story)            (S000007)                     templates/
                                                                reference/

"Here is HOW we work."            "Check THIS item."            "Structural rules and examples."
```

## User Stories

### P0 (Must-Have)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 1 | core | Ambient workflow awareness | engineer | have Copilot know our lifecycle + IDs | it answers scaffolding questions correctly without me re-explaining |
| 2 | core | Scaffolding guidance | engineer | get directory layouts that match the spec | `work-items/features/{ID}_{slug}/` is respected automatically |
| 3 | core | Point to the validator | engineer | have Copilot tell me to run `/validate` | conventions are enforced, not just remembered |
| 4 | core | Stay within budget | engineer | keep the file small enough to always fit | the file never gets stripped for token budget |

### P1 (Important)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 5 | usability | Common tasks index | engineer | see a table of 5–10 common tasks mapped to prompts/docs | I don't have to hunt through reference docs |
| 6 | observability | Cite sources inside instructions | maintainer | have each claim link to its authority | drift is auditable |

### P2 (Nice-to-Have)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 7 | integration | Mention sibling prompts | engineer | know `/validate` exists (and future prompts) | the bundle is discoverable from one file |

## Acceptance Criteria

### Story #1: Ambient workflow awareness [core]

```
GIVEN a target repo with `.github/copilot-instructions.md` installed
WHEN  I ask Copilot "how do I add a user story under F000004?"
THEN  it proposes `work-items/features/F000004_*/S0000NN_*/` with correct ID increment
AND   it mentions required artifacts (TRACKER, PRD, ARCHITECTURE, TEST-SPEC)
```

### Story #2: Scaffolding guidance [core]

```
GIVEN the instructions file includes directory layout rules
WHEN  I ask "where does a defect live?"
THEN  Copilot answers `work-items/defects/D000NNN_{slug}/` with TRACKER + RCA + test-plan
```

### Story #3: Point to the validator [core]

```
GIVEN an engineer asks "is my work item compliant?"
WHEN  Copilot answers
THEN  it instructs them to run `/validate <path>` rather than hand-checking
```

### Story #4: Stay within budget [core]

```
GIVEN the final instructions file
WHEN  I measure its byte size
THEN  size <= 8192 bytes (8 KB)
AND   no single section exceeds 1 KB
```

## Success Metrics

| Metric | Target | How Measured |
|--------|--------|-------------|
| File size | <= 8 KB | `wc -c copilot-instructions.md` |
| Convention accuracy | 100% match against `WORKFLOW.md` | Manual spot-check of every claim |
| User trust | subjective | engineer reports "Copilot now proposes the right path first try" |

## Out of Scope

- Embedding the manifest inline (too large; already on disk)
- Teaching Copilot to author full trackers (that's the `/validate` prompt's cousin, not this story)
- Per-repo customization (single canonical instructions file; repos inherit as-is)

## Assumptions

- Copilot respects `.github/copilot-instructions.md` on the target work machine
- The user's workflow won't diverge significantly between the Claude Code
  workbench and the work machine; if it does, we fork the file.

<!-- ===== From S000009_ARCHITECTURE.md ===== -->

## Overview

A single markdown file (`copilot-instructions.md`) sits in
`work-copilot/instructions/` as the source of truth and is installed to
`<target>/.github/copilot-instructions.md` by S000008's installer. Content is
a compact index of conventions — not a duplicate of the manifest or the
validate prompt — and ends with a pointer to `/validate` for enforcement.

## Architecture

```
work-copilot/instructions/copilot-instructions.md    (source of truth)
         |
         | installed verbatim by copilot-deploy install
         v
<target>/.github/copilot-instructions.md             (always-on in Copilot chat)
         |
         | Copilot loads implicitly at every request
         v
all Copilot chats in target repo see it
```

Structure of the file (planned) — 5 sections as H2 headers in the
installed `copilot-instructions.md`:

1. `Working on this repo (ambient context)` — intro, one paragraph
2. `How work is tracked` — hierarchy (feature > user-story > task, depth 3),
   IDs (F/S/T/D + 6 digits), placement rules, 3-phase lifecycle (Track,
   Implement, Ship)
3. `How to add a work item` — pointer to `.github/work-copilot/templates/`
   and minimum required artifacts per type from the manifest
4. `How to check compliance` — "run `/validate <path>`"
5. `Sources` — links to manifest path, templates dir, and upstream
   `skills/personal-workflow/WORKFLOW.md`

### Components Affected

| Component | Repo | Change Type | Description |
|-----------|------|------------|-------------|
| `work-copilot/instructions/copilot-instructions.md` | claude-skills-templates | New | The always-on source file |
| `work-copilot/install-manifest.json` | claude-skills-templates | Modified | Include the new instructions file and its install target |

### Data Flow

1. Engineer opens target repo in VS Code
2. Copilot Chat loads `.github/copilot-instructions.md` into every request
3. When user asks a workflow question, Copilot has ambient context to
   answer correctly and pointers to load more if needed
4. For any actual compliance check, it invokes `/validate`

## API Changes

### New APIs

| API | Signature | Description |
|-----|-----------|-------------|
| `.github/copilot-instructions.md` | File | Always-on context for Copilot Chat |

### Modified APIs

| API | Before | After | Reason |
|-----|--------|-------|--------|
| `install-manifest.json` | 3 file categories (prompts, templates, reference) | 4 categories (+ instructions) | New install target path |

## Dependencies

| Dependency | Type | Status | Notes |
|-----------|------|--------|-------|
| S000008 installer | Feature | Pending | Needed to actually deliver the file to the target repo |
| Copilot `.github/copilot-instructions.md` support | Tool | Available | Standard Copilot Chat feature |

## Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Instructions drift from WORKFLOW.md | Med | Med | Add a Tier 1 smoke test that greps for known invariants (ID regex, phase names, filenames) |
| Byte budget exceeded as spec grows | Low | Low | Tier 1 size check; trim or link out if over |
| Copilot ignores instructions in some contexts (e.g., inline suggestions vs chat) | Med | Low | Document scope: this only influences Chat, not inline completion |
| Conflicting instructions in user's pre-existing `copilot-instructions.md` | Med | High | S000008 installer refuses to overwrite a non-bundle file without `--overwrite` (drift detection) |

## Design Decisions

| Decision | Chosen | Rejected Alternative | Why |
|----------|--------|---------------------|-----|
| Single file | One `copilot-instructions.md` | Split across multiple files + `.copilot/` dir | Copilot only loads the canonical path; splitting loses always-on guarantee |
| Content shape | Index + pointers | Full spec inline | Budget + single-source-of-truth; manifest and templates stay authoritative |
| Validator mention | Explicit `/validate` pointer | Let Copilot improvise | Forces the compliance path; no invention |
| Source of truth | Maintained in `work-copilot/instructions/` and copied on install | Generated on install from WORKFLOW.md | Simpler and reviewable; can be generated later if worth it |
