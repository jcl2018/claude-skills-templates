---
type: test-plan
parent: T000013_migrate_company_workflow
title: "Migrate company-workflow — Test Plan"
date: 2026-05-02
author: chjiang
status: Draft
---

<!-- Scope: ONE fix (defect) or ONE task. Cases must be concrete and reproducible.
     For broader coverage of a user story, use TEST-SPEC.md instead.
     For defects, the test cases are regression cases for the specific bug. -->

## Scope

This task flips the `company-workflow` entry in `skills-catalog.json` from `status: "active"` to `status: "deprecated"`, regenerates `README.md`, and verifies that the install/doctor behavior introduced by S000012 actually does the right thing on a clean target. No source files under `skills/company-workflow/` are modified — those stay as upstream truth for the `work-copilot/` byte-mirror.

Files modified:
- `skills-catalog.json` (1 entry's `status` field)
- `README.md` (regenerated by `scripts/generate-readme.sh`)

## Regression Test Cases

| # | Test Case | Steps | Expected Result | Status |
|---|-----------|-------|-----------------|--------|
| 1 | Clean install excludes company-workflow | `rm -rf ~/.claude/skills/company-workflow`; `scripts/skills-deploy install`; `ls ~/.claude/skills/` | `company-workflow/` absent; other skills present; stdout/stderr contains exactly one `WARN: skipping deprecated skill: company-workflow (use --include-deprecated to install)` line | Pass |
| 2 | Opt-in install includes company-workflow | (after #1) `scripts/skills-deploy install --include-deprecated`; `ls ~/.claude/skills/` | `company-workflow/` present; no WARN line for it; install summary reports it as installed | Pass |
| 3 | Doctor under deprecated-not-installed | (after a fresh `rm -rf ~/.claude/skills/company-workflow`) `scripts/skills-deploy doctor` | INFO line for `company-workflow` mentioning "deprecated — not installed by default"; no WARN; exit code 0 | Pass |
| 4 | Doctor under deprecated-installed | (after #2) `scripts/skills-deploy doctor` | INFO line for `company-workflow` mentioning "deprecated — installed (--include-deprecated)"; exit 0 | Pass |
| 5 | Validate accepts the new status | `scripts/validate.sh` | Exit 0; no errors mentioning `company-workflow` or `status` | Pass |
| 6 | Generated README has Deprecated section | `scripts/generate-readme.sh > /tmp/new-readme.md`; `grep -E '^## Deprecated' /tmp/new-readme.md`; `grep 'company-workflow' /tmp/new-readme.md` | Both greps match; `company-workflow` row appears under the Deprecated section, not the active table | Pass |
| 7 | work-copilot byte-mirror still passes | `scripts/validate.sh` (Error check 10's `MIRROR_SPECS` array) | Exit 0; no mirror drift between `skills/company-workflow/` and `work-copilot/` | Pass |
| 8 | Pre-existing install is not removed by filter | `mkdir -p ~/.claude/skills/company-workflow`; `touch ~/.claude/skills/company-workflow/SENTINEL`; `scripts/skills-deploy install` (no flag); `ls ~/.claude/skills/company-workflow/` | Sentinel still present; install printed WARN line but did not remove anything | Pass |
| 9 | Remove on previously-installed deprecated works | (after #2) `scripts/skills-deploy remove company-workflow`; `ls ~/.claude/skills/` | `company-workflow/` no longer present; no errors | Pass |
| 10 | Full test suite still green | `./scripts/test.sh` | Exit 0 | Pass |

## Verification Steps

<!-- How was the fix verified beyond the test cases above? -->

- [x] Local build / scripts succeed on macOS (the development platform)
- [x] `/personal-workflow check work-items/features/F000005_deprecated_skill_status/` passes with no findings (validate.sh manifest reconciliation: all 10 work-item rows PASS)
- [x] `git diff skills-catalog.json` shows ONLY the one field change (`"active"` → `"deprecated"` on the `company-workflow` entry)
- [x] `git diff README.md` matches the expected shape: removal of `company-workflow` row from active table (line 12); addition of `### Deprecated` section + 1-row table (lines 13-19)
- [x] No new files under `skills/company-workflow/` were modified or removed (deprecation ≠ deletion)
- [x] `work-copilot/` directory unchanged (`git diff work-copilot/` is empty)

## Environments Tested

| Environment | Build | Result |
|------------|-------|--------|
| macOS (zsh, bash 3.2.57) | feat/deprecated-skill-status | Pass |
