---
type: prd
parent: S000003_company_workflow_implementation
feature: F000003_company_spec_system
title: "Company Workflow Implementation — Product Requirements"
version: 2
status: Draft
date: 2026-04-11
author: chjiang
reviewers: []
---

## Problem Statement

The company follows a formal work item specification: structural validation rules
(contract.json), 5 tracker types with Jira/TFS integration, 8 doc types, reference
guides for AI doc generation, and philosophy docs explaining lifecycle rationale.
Today this spec lives at `~/Downloads/spec/` with no enforcement. Engineers follow
it manually, work items drift from the spec, and nobody notices until review time.

The company-workflow skill packages this entire spec as a standalone Claude Code skill.
Install it in any repo. No gstack. No /review, /ship, /docs check. It just works.

## Mental Model

```
company-workflow (standalone, zero external deps)
    |
    +-- validate <file>    contract.json structural rules -> exit 0/1
    +-- validate <dir>     artifact completeness per type -> [PASS]/[MISSING]/[DRIFT]
    |
    reads: contract.json, company-artifact-manifests.json, templates/company-workflow/
    writes: nothing (read-only)
    depends on: nothing external
```

One command, two modes. Pass a file for structural validation, pass a directory for
artifact completeness.

- **File mode**: checks a single file against contract.json (required fields, sections, order, 4-phase lifecycle). Exit 0/1.
- **Directory mode**: checks a work item directory for artifact completeness (are all required files for the type present? correct frontmatter? no unresolved placeholders?). Reports [PASS]/[MISSING]/[DRIFT].

## Lifecycle Boundary

The company spec uses a 4-phase lifecycle (Track, Implement, Review, Ship). The
workbench uses 3-phase. This is intentional divergence. The skill preserves the
company spec exactly, including `workflow_type`, `url`, verbose sub-gate checkboxes,
and the review type.

## Company Template Inventory (13 files)

### Tracker templates (5)

| File | Type | Fields | Notes |
|------|------|--------|-------|
| tracker-feature.md | feature | 11 (incl. workflow_type, url) | 4-phase, 16 sub-gates |
| tracker-defect.md | defect | 11 | "Root cause identified", "Hypothesis tested" |
| tracker-task.md | task | 12 (incl. parent) | Only tracker with parent |
| tracker-user-story.md | userstory | 11 | Type is `userstory` (no hyphen) |
| tracker-review.md | review | 12 (incl. deadline) | Meetings + Handoff sections |

### Doc templates (8)

| File | Type | Fields | Notes |
|------|------|--------|-------|
| doc-PRD.md | prd | 9 | P0/P1/P2 story tables |
| doc-ARCHITECTURE.md | architecture | 10 | ASCII diagram, Components table |
| doc-TEST-SPEC.md | test-spec | 11 | Two-tier test model |
| doc-milestones.md | milestones | 5 | Table + dependency graph |
| doc-RCA.md | rca | 7 (incl. severity) | Investigation trail |
| doc-test-plan.md | test-plan | 6 | Simplest template |
| doc-review-notes.md | review-notes | 10 (incl. verdict) | Most frontmatter fields |
| doc-scrum.md | scrum-meeting | 7 (incl. attendees array) | Array-valued frontmatter |

## User Stories

### P0 (Must-Have)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 1 | validate | Catch missing fields | engineer | see violations when required fields absent | I fix problems before review |
| 2 | validate | Check section order | engineer | see violations when sections out of order | trackers match the spec |
| 3 | validate | Check lifecycle | engineer | see violations when phases missing | lifecycle matches the spec |
| 4 | check | Detect missing artifacts | engineer | see [MISSING] for absent docs | incomplete items caught |
| 5 | check | Detect frontmatter drift | engineer | see [DRIFT] for wrong fields | structural compliance enforced |
| 6 | check | Detect section drift | engineer | see [DRIFT] for missing sections | structure matches templates |
| 7 | check | Cover all 5 types | engineer | see correct requirements per type | every type validated |
### P1 (Important)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 8 | validate | Detect unresolved placeholders | engineer | see [DRIFT] for {PLACEHOLDER} in values | no unfilled templates |
| 9 | deploy | Deploy to any repo | engineer | install via skills-deploy | any repo gets the full spec |
| 10 | reference | Access generation guides | engineer | read guides for AI doc generation | AI follows conventions |

## Acceptance Criteria

### Stories #1-3: Validate [shipped]

```
GIVEN a company tracker exists
WHEN  I run company-workflow validate
THEN  exit 0 if all contract.json rules pass
AND   exit 1 with violations on stderr
AND   checks: required fields, section order, lifecycle phases, min checkboxes
```

### Stories #4-7: Directory Mode Validate

```
GIVEN a feature directory with tracker but no PRD
WHEN  I run company-workflow validate on that directory
THEN  output includes [MISSING] PRD.md

GIVEN an artifact missing workflow_type in frontmatter
WHEN  I run company-workflow validate on that directory
THEN  output includes [DRIFT] — missing required field

GIVEN one complete work item directory per type
WHEN  I run company-workflow validate on each directory
THEN  feature=5, defect=3, task=2, userstory=5, review=2 required
AND   complete items get [PASS] for all artifacts
```

## Success Metrics

| Metric | Target | How Measured |
|--------|--------|-------------|
| Types enforced by validate (dir mode) | 5/5 | Run validate on each type directory |
| File mode validate | All fixture types | validate exit 0 on valid, exit 1 on invalid |
| Portability | Works in non-gstack repo | cp skill to temp repo, run validate |

## Out of Scope

- Automated sync from company spec upstream
- Converting workbench work items to company format
- Scaffolding / create command (use workbench work-items.md rules for scaffolding)
- Hierarchy / placement rules (use /docs check for that)
- Recursive directory validation (validate each directory separately)
- Any gstack integration (analytics, /review, /ship)

## Assumptions

- The repo is the source of truth after template import (done)
- skills-deploy can handle subfolder templates (or follow-up patches)
