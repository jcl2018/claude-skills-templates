---
type: spec
parent: S000001
feature: F000001
title: "Workflow Alpha Implementation — Specification"
version: 1
status: Draft
date: 2026-04-11
author: chjiang
reviewers: []
---

<!-- Migrated from S000001_PRD.md + S000001_ARCHITECTURE.md during F000008
     v1.5.0 sweep. Section content preserved from the two source files
     for historical fidelity. The new doc-SPEC.md template suggests
     Problem Statement / Mental Model / ## Requirements (with ### P0/P1/P2
     sub-sections) / Acceptance Criteria / Architecture / Tradeoffs /
     Open Questions; refine over time as needed. -->

<!-- ===== From S000001_PRD.md ===== -->

## Problem Statement

Claude Code skill development lacks a structured lifecycle. Work items are created ad-hoc, tracker templates assume multi-person teams (reviewers, Linux builds, JIRA URLs), and there is no validation that work item hierarchies are structurally complete. A developer working solo with Claude Code has no quality gates that match their actual workflow, no type-specific artifact sets, and no way to verify that features are decomposed into actionable work.

## Mental Model

Three layers of capability, each building on the previous:

1. **Solo-dev templates**: 4 tracker templates rewritten with a 3-phase lifecycle (Track -> Implement -> Ship) and gates meaningful for a single developer + Claude Code. Type-specific artifact scaffolding via artifact-manifests.json. `/ship` runs pre-landing code review internally, so a separate Review phase is unnecessary.
2. **Structural completeness** (Check 4 in /docs check): Does the hierarchy exist? Feature -> stories -> tasks. Validates placement rules, detects orphans and misplaced items.
3. **Visualization and artifacts**: Tree report with per-node badges, machine-readable graph artifact for downstream tooling, human-readable report.

## User Stories

### P0 (Must-Have)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 1 | core | Do templates fit solo-dev? | developer | use tracker templates without multi-person ceremony | gates are meaningful for my actual workflow |
| 2 | core | Does each type get the right artifacts? | developer | have features get tracker only, user-stories get PRD+ARCH+TEST-SPEC+milestones, defects get RCA+test-plan | each type gets appropriate docs |
| 3 | core | Are structured IDs enforced? | developer | use F/S/D/T prefix with 6-digit numbers | work items have consistent, sortable IDs |
| 4 | core | Does the check catch incomplete features? | developer | run /docs check and see features without stories flagged as INCOMPLETE | I know which features need decomposition |
| 5 | core | Can I see the full hierarchy at a glance? | developer | see a tree report with per-node badges | I can assess work item health visually |
| 6 | core | Is there a machine-readable output? | developer | get a work-item-graph.json artifact | downstream tools can consume work item state |
| 7 | core | Are hierarchy rules configurable? | developer | define hierarchy rules in artifact-manifests.json | different repos can customize structural requirements |

### P1 (Important)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 8 | usability | Can I get just the tree quickly? | developer | run /docs tree for a fast hierarchy view | I don't have to run all checks just to see the tree |
| 9 | core | Are misplaced items caught? | developer | see tasks directly under features flagged as MISPLACED | I catch hierarchy violations |
| 10 | observability | Does lifecycle match structure? | developer | see LIFECYCLE_INCONSISTENT when "broken down" is checked but 0 children exist | contradictory signals are surfaced |
| 11 | resilience | Does it work without narrative docs? | developer | run structural checks even without .docs/claims.json | I don't need /docs init first |

### P2 (Nice-to-Have)

| # | Tag | What it asks | As a... | I want to... | So that... |
|---|-----|-------------|---------|-------------|------------|
| 12 | usability | Can I see completeness counts? | developer | see child counts vs minimum in the tree report | I know how decomposed each feature is |

## Acceptance Criteria

### Story #1-3: Solo-dev templates and scaffolding [core]

- AC-1: No "reviewer noted", "Linux branch build", JIRA/TFS URL, or workflow_type in templates
- AC-2: Lifecycle gates meaningful for single developer + Claude Code
- AC-3: Task tracker lighter than feature tracker (fewer gates)
- AC-4: All 4 tracker templates updated consistently
- AC-5: Structured IDs: F/S/D/T prefix with 6-digit number + keywords
- AC-6: Review work item type removed (tracker-review.md deleted)
- AC-7: Scrum removed (subcommand, template, notes generation)
- AC-8: Phase 1 lists required artifacts per type (feature: tracker only, user-story: PRD+ARCH+TEST-SPEC+milestones, task: test-plan, defect: RCA+test-plan)

### Story #4: Incomplete feature/story detection [core]

- AC-9: Feature with 0 user-story children flagged INCOMPLETE
- AC-10: Feature with >= 1 user-story children shows PASS
- AC-11: User-story with 0 task children flagged INCOMPLETE

### Story #5: Tree report [core]

- AC-12: Tree renders depth-first, sorted alphabetically by slug
- AC-13: Each node shows 4 badges (template, lifecycle, traceability, structure)
- AC-14: Badge shows worst severity from its check category

### Story #6: Graph artifact [core]

- AC-15: work-item-graph.json written to .docs/ with version, nodes, edges, structural_rules
- AC-16: Each node includes id, slug, type, state, path, parent, children, badges, completeness

### Story #7: Configurable hierarchy [core]

- AC-17: Hierarchy rules read from artifact-manifests.json hierarchy field
- AC-18: Missing hierarchy field warns and skips structural checks

### Story #8: /docs tree subcommand [usability]

- AC-19: /docs tree renders tree with structural badges only; other badges show "-"

### Story #9: Misplaced detection [core]

- AC-20: Item whose type is not an allowed child of its parent flagged MISPLACED
- AC-21: Placement rules: feature/defect root-level, story inside feature, task inside story

### Story #10: Lifecycle cross-reference [observability]

- AC-22: "Broken down" checked + 0 children = LIFECYCLE_INCONSISTENT

### Story #11: Claims.json gate [resilience]

- AC-23: Steps 1-5 skip with note if claims.json missing; Steps 6+ run regardless

## Success Metrics

- Test suite passes with 0 failures after all changes
- /docs check flags F000002 as INCOMPLETE on first run
- /docs tree renders in under 5 seconds on repos with < 50 work items
- Zero false positives on correctly structured hierarchies (F000001)

## Out of Scope

- Dependency edges (blocked-by, depends-on) in graph artifact — deferred to v2
- validate.sh integration with graph artifact — deferred until schema stable
- Auto-scaffolding of missing children — validation only, no enforcement

## Assumptions

- Work items follow the directory nesting convention from rules/work-items.md
- artifact-manifests.json is the single source of truth for hierarchy rules and artifact sets
- Solo developer + Claude Code is the primary workflow

<!-- ===== From S000001_ARCHITECTURE.md ===== -->

## Overview

Three interconnected changes to the skill development workbench:

1. **Template consolidation**: All 4 tracker templates rewritten for solo-dev workflow. Removed review work item type, scrum, multi-person ceremony. Added structured IDs. Phase 1 lists required artifacts per type. Phase 2 for features/stories coordinates children; tasks/defects do direct implementation.
2. **Structural completeness** in /docs check: Three new steps (15-17) validate hierarchy, render a tree report, and emit a graph artifact. Plus /docs tree as a standalone subcommand.
3. **Human-readable report**: Step 19 writes .docs/work-item-report.md after all checks complete.

## Architecture

```
artifact-manifests.json
    │
    ├── types     -> per-type required artifacts + templates
    ├── hierarchy -> {feature->user-story (min 1), user-story->task (min 1)}
    └── placement -> {feature:root, defect:root, story:feature, task:story}
            │
            ▼
skills/docs/check.md
    │
    ├── Steps 1-5:  Staleness checks (skip if no claims.json)
    ├── Steps 6-14: Template, lifecycle, traceability checks
    ├── Step 15:    Structural completeness + orphan/misplaced detection
    ├── Step 16:    Tree report (depth-first, 4 badges per node)
    ├── Step 17:    Graph artifact (.docs/work-item-graph.json)
    └── Step 19:    Human-readable report (.docs/work-item-report.md)
            │
            ▼
skills/docs/tree.md -> Step 15 + Step 16 (structural badges only, others show "-")
```

### Components

| Component | Change Type | Description |
|-----------|------------|-------------|
| skills/docs/check.md | Modified | Add Steps 15-17, Step 19, separate claims.json gate, badge taxonomy |
| skills/docs/tree.md | New | Standalone tree subcommand |
| skills/docs/SKILL.md | Modified | Add /docs tree routing |
| artifact-manifests.json | Modified | Add hierarchy and placement fields |
| skills-catalog.json | Modified | Version bumps, add tree.md |
| templates/tracker-feature.md | Modified | Solo-dev gates, required doc lists, coordination Phase 2 |
| templates/tracker-user-story.md | Modified | Solo-dev gates, required doc lists, coordination Phase 2 |
| templates/tracker-task.md | Modified | Solo-dev gates, required doc lists, implementation Phase 2 |
| templates/tracker-defect.md | Modified | Solo-dev gates, required doc lists, /investigate + RCA |

### Removed

| File | Reason |
|------|--------|
| templates/tracker-review.md | Review work item type eliminated |
| templates/contract-PRD.md | Unused enforcement templates |
| templates/contract-ARCHITECTURE.md | Unused enforcement templates |
| templates/contract-TEST-SPEC.md | Unused enforcement templates |
| templates/GENERATION-GUIDE.md | Dead template, replaced by artifact-manifests.json |
| templates/*-GENERATION-GUIDE.md (3) | Dead templates |

### Data Flow

1. check.md reads artifact-manifests.json for hierarchy rules
2. Step 10 walks work-items/ recursively, builds Actual Model (type, parent, children, state per item)
3. Steps 11-14 run checks 1-3, accumulating badges per node
4. Step 15 reads hierarchy rules, counts children per type, flags INCOMPLETE/MISPLACED, checks placement rules
5. Step 16 calculates completeness counts, renders depth-first tree with all 4 badges per node
6. Step 17 serializes graph to .docs/work-item-graph.json
7. Step 19 writes human-readable report to .docs/work-item-report.md

### Badge Taxonomy

| Badge category | Status values (severity order) |
|---------------|-------------------------------|
| template | PASS < WARN (EXTRA) < DRIFT (missing field/section) < INCOMPLETE (MISSING artifact) |
| lifecycle | PASS < WARN < LIFECYCLE_INCONSISTENT |
| traceability | PASS < INFO (P1/P2 untested) < UNTESTED (P0 untested) |
| structure | PASS < INCOMPLETE < MISPLACED |

### Graph Artifact Schema (v1.0.0)

```json
{
  "version": "1.0.0",
  "generated_at": "ISO-8601",
  "generated_commit": "short-SHA",
  "nodes": [{
    "id": "F000001", "slug": "...", "type": "feature", "state": "Open",
    "path": "...", "parent": null, "children": [...],
    "badges": {"template": "PASS", "lifecycle": "PASS", "traceability": "PASS", "structure": "PASS"},
    "completeness": {"count": 1, "min": 1, "required_child": "user-story"}
  }],
  "edges": [],
  "structural_rules": {"feature": {"required_child": "user-story", "min": 1}, "user-story": {"required_child": "task", "min": 1}}
}
```

### Template Consolidation

| Change | Before | After |
|--------|--------|-------|
| Ship phase gates | "Reviewer noted findings", "Linux branch build passed" | /docs check + TEST-SPEC/test-plan verification + /ship (includes pre-landing review) + /land-and-deploy |
| ID format | Free-text | F/S/D/T + 6-digit + keywords |
| Phase 1 docs | Generic "produce docs" | Type-specific required doc list per artifact-manifests.json |
| Phase 2 (feature/story) | Direct implementation | Coordinates children |
| Phase 2 (task/defect) | Generic | Direct implementation with design doc reference |
| Multi-person fields | JIRA URL, workflow_type, reviewers | Removed |

## Error Handling

| Error condition | Response |
|----------------|----------|
| hierarchy field missing from manifest | Warn, skip structural checks |
| No work-items/ directory | Skip all work item checks |
| No claims.json | Skip Steps 1-5, continue to Step 6 |
| "Broken down" checked + 0 children | Flag LIFECYCLE_INCONSISTENT |

## Design Decisions

1. **Hierarchy rules in manifest, not hard-coded**: Enables per-project customization. Required field forces repos to be explicit.
2. **Badge taxonomy with severity ordering**: Worst-severity aggregation gives single-glance health.
3. **Separate /docs tree**: Quick structural view without running all check steps.
4. **Solo-dev gates over multi-person ceremony**: Gates like "reviewer noted findings" are meaningless for a solo developer.
5. **Feature/story Phase 2 coordinates children**: Implementation happens in tasks and defects (leaf nodes), not in features or stories.

## Historical Note

An intermediate /workflow router skill was built during development and removed in v0.2.2. Its track phase was replaced by CLAUDE.md rules (rules/work-items.md). Its implement, review, and ship phases were redundant with gstack /office-hours, /review, and /ship.
