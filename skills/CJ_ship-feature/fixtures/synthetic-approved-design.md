# Design: synthetic fixture for /CJ_ship-feature smoke test

Generated by fixture, not /office-hours
Branch: synthetic-fixture
Repo: jcl2018/claude-skills-templates
Status: APPROVED
Mode: Builder

## Problem Statement

This is a fixture design doc, not a real one. The wrapper `/CJ_ship-feature`
uses it to exercise its pre-flight + Phase 1 plumbing without requiring a
real /office-hours run. Real `Status: APPROVED` so the pre-flight grep passes;
minimal content so /autoplan has SOMETHING to review without burning tokens.

## What Makes This Cool

Nothing. It's a fixture. The cool thing is that the wrapper exists at all.

## Constraints

- Must contain `Status: APPROVED` as a line beginning with `Status:` (Step 1
  pre-flight greps for this).
- Must be under `~/.gstack/projects/` when invoked (Step 1 enforces path).
  Tester copies this file to `~/.gstack/projects/scratch/...` before invoking
  the wrapper.

## Premises

- /autoplan will review this and may surface a final-approval AUQ with low
  confidence (the doc is too thin for high confidence). That's fine — the
  test is exercising plumbing, not design quality.

## Approaches Considered

### Approach A (CHOSEN): Synthetic fixture, minimum valid shape
One markdown file with the required sections. Tester copies to
`~/.gstack/projects/scratch/` and invokes the wrapper.

### Approach B: Full /office-hours run for each test
Run /office-hours on a real (tiny) topic each time. Cost: 5-10 min per smoke.
Rejected: too expensive for routine pre-flight tests.

## Recommended Approach: A

## Open Questions

None — this is a fixture.

## Success Criteria

- /CJ_ship-feature pre-flight Step 1 accepts this file (does not refuse on
  missing `Status: APPROVED`).
- /autoplan's review chain runs without crashing.
- If tester continues past /autoplan's final-approval AUQ, Phase 2 dispatches
  the CJ_personal-pipeline subagent (which may halt at scaffold gate if the
  design is too thin to produce a valid work-item — that's the expected
  failure shape for a contrived fixture).

## Distribution Plan

Not applicable — this is a fixture design doc, not a real change.

## Dependencies

- /CJ_ship-feature (the wrapper being tested)
- /autoplan, /CJ_personal-pipeline, /ship, /land-and-deploy (transitive deps
  of the wrapper)
